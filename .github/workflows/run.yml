name: Run

on:
  workflow_dispatch: {}

jobs:
  run:
    name: linux
    runs-on: ubuntu-24.04
    steps:
      - name: Deps
        run: |
          sudo apt-get update
          sudo apt-get -y install xfvb
